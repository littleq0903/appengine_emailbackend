from distutils.core import setup

setup(name="AppEngine Email Backend",
        version="1.0",
        description="Django wrapper for App Engine email APIs.",
        packages=['appengine_emailbackend'],
        )
